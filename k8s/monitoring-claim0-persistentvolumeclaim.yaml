apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: monitoring-claim0
  name: monitoring-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
